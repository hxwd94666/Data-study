
---

### 🎯 Day 3 核心目标 Checklist（今日保底任务）

* [ ] **指法强化**：完成 30 分钟盲打，重点攻克 Python 高频符号（`_`, `{}`, `[]`, `:`）。
* [ ] **欠账清零**：补上看黑马 Python 视频，并在 IDEA 中盲打跑通字典（Dict）与函数（Function）代码。
* [ ] **算法巩固**：在 IDEA 用企业级规范（Type Hint + Docstring）完成力扣 283 题（移动零）。
* [ ] **SQL 跃迁**：初探大厂数据开发必考神技——**窗口函数 (Window Function)**。
* [ ] **开源连胜**：用规范的 `feat:` 前缀提交代码，保持 GitHub 绿草地。

---

### ⏰ Day 3 详细执行日程与资源指南

#### 🌅 上午：指法重塑与 Python 核心补天（9:00 - 11:30）

**【保底任务：约 2.5 小时】**

* **09:00 - 09:30 | ⌨️ 盲打特训**
* **行动**：Keybr.com 30 分钟。今天留意一下右手小指够大括号 `{}` 和中括号 `[]` 的感觉，这是接下来写 Python 字典的必备动作。


* **09:30 - 10:30 | Python 欠账清零（字典与集合）**
* **行动**：打开昨天没看的“黑马程序员 Python教程”（字典与集合篇）。在 IDEA 项目里新建 `day03_dict_set.py`。
* **核心实操**：用极慢的盲打敲出一个包含你个人信息的字典，并尝试用 `for` 循环遍历它的 `keys()` 和 `values()`。


* **10:30 - 11:30 | Python 函数进阶**
* **行动**：继续看“函数参数与返回值”部分。学会写 `def process_data(data: dict) -> list:` 这种标准结构。体会局部变量的作用域。



#### ☀️ 下午：算法连击与 SQL 核武器初探（14:00 - 17:30）

**【保底任务：约 2.5 小时】**

* **14:00 - 15:00 | 算法第三战：快慢双指针**
* **学习资源**：力扣 **第 283 题（移动零）**。《代码随想录》-> 数组 -> 移除元素（里面有这道衍生题的精讲）。
* **行动**：在 IDEA 新建 `day03_move_zeroes.py`。这题是昨天“移除元素”的孪生兄弟。必须套用 `class Solution:`、`self` 以及企业级注释。敲完提交到力扣测全绿。


* **15:00 - 16:30 | 🔥 SQL 跃迁：窗口函数 (极其重要)**
* **概念预警**：大厂面试中，80% 的 SQL 压轴题考的都是它。普通的 `GROUP BY` 会把多行聚合缩成一行，但窗口函数可以在**不改变原有行数**的情况下，为每一行数据计算排名或累计值。
* **学习资源**：B站搜索“SQL 窗口函数”，找个 10 分钟的动画演示看懂概念。然后去牛客网 -> SQL进阶挑战 -> 找带有 `ROW_NUMBER()` 的基础题。
* **行动**：今天只学最核心的一个语法：`ROW_NUMBER() OVER (PARTITION BY 部门 ORDER BY 薪水 DESC)`（计算每个部门内的薪水排名）。在本地 `day03_window_func.sql` 盲打练习拼写。



**【🔥 附加任务：如果你在 16:30 前完成】**

* **极客扩展（+30分钟）**：去查一下另两个窗口函数 `RANK()` 和 `DENSE_RANK()` 与 `ROW_NUMBER()` 的区别（面试超高频必问：并列第一时，第二个人排第几？）。

#### 🌙 晚上：沉淀复盘与 GitHub 提交（19:00 - 21:00）

**【保底任务：约 1 小时】**

* **19:00 - 19:30 | Markdown 深度复盘**
* **行动**：新建 `Day03.md`。今天复盘的重点是：**用自己的话（中文盲打）把什么是“窗口函数”解释一遍。** 顺便记录一下 Python 字典操作最容易打错的符号。


* **19:30 - 20:00 | 企业级 Git 提交**
* **行动**：打开 IDEA 终端，依次盲打执行：
1. `git add .`
2. `git commit -m "feat(sql): 初探 ROW_NUMBER 窗口函数，补全 Python 字典与函数用法"`
3. `git push`





---

# 📅 Day 3: Python 数据结构遍历与 SQL 窗口函数破冰

## 🎯 核心目标 Checklist (Core Objectives)
- [x] **键盘基建**：完成 30 分钟纯盲打指法训练，强化特殊符号敲击准确率。
- [x] **Python 结构**：在 IDE 中独立编写并测试字典（Dict）与集合（Set）的遍历逻辑与函数封装。
- [x] **算法巩固**：遵循 Type Hint 与 Docstring 规范，手写通关 LeetCode 283 (移动零)。
- [x] **SQL 跃迁**：掌握 `ROW_NUMBER()` 窗口函数的核心语法与分区排序逻辑。
- [x] **开源规范**：使用 Conventional Commits 规范，将今日产出推送至 GitHub。

## ⏰ 详细执行日程与技术路线 (Execution Plan)

### 🌅 上午：指法重塑与 Python 核心结构 (09:00 - 11:30)
* **09:00 - 09:30 | ⌨️ 盲打特训**
  * 核心动作：针对 Python 高频符号 `{}`、`[]`、`_` 进行无视觉依赖的盲打强化。
* **09:30 - 11:30 | 🐍 Python 字典与函数进阶**
  * 技术聚焦：字典键值对的遍历方法（`keys()`, `values()`, `items()`）与集合的去重特性。函数的参数定义与局部作用域。
  * 产出目标：在 `First_month/day03_dict_set.py` 中编写包含真实数据结构的测试脚本。

### ☀️ 下午：算法连击与 SQL 核武器初探 (14:00 - 17:30)

* **14:00 - 15:00 | 🧠 算法实战：快慢双指针**
  * 技术聚焦：运用快慢双指针思想实现原地数组操作，降低空间复杂度至 O(1)。
  * 产出目标：在 `First_month/day03_move_zeroes.py` 中完成 LeetCode 283 题的企业级代码编写与测试。
* **15:00 - 16:30 | 🗄️ SQL 跃迁：窗口函数 (Window Functions)**
  * 技术聚焦：理解窗口函数“计算排名但不改变原有行数”的底层逻辑。掌握 `OVER (PARTITION BY ... ORDER BY ...)` 语法体系。
  * 产出目标：完成 LeetCode 178 题，在 `First_month/day03_window_func.sql` 中编写基础的分区排序查询逻辑。

### 🌙 晚上：沉淀复盘与 GitHub 提交 (19:00 - 21:00)
* **19:00 - 19:30 | 📝 技术深度复盘**
  * 核心动作：提炼窗口函数的业务应用场景，总结今日代码调试过程中的语法易错点。
* **19:30 - 20:00 | 🚀 规范化提交**
  * 核心动作：执行 `git add .` -> `git commit -m "feat(sql/python): 引入 ROW_NUMBER 窗口函数，实现字典遍历与双指针算法"` -> `git push`。

---

## 🗣️ 面试预演 (Interview Simulation)
> **Scenario**: 业务方需要一张包含全国销售人员业绩的流水表，要求在**不改变原表数据行数**的前提下，新增一列显示每个省份内部的销售额排名。
> **Question**: 请手写这段 SQL 的核心逻辑，并解释 `PARTITION BY` 与 `GROUP BY` 在引擎执行时的本质区别。

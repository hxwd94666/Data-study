### 📅 Week 5 深度复盘日：资产审计与逻辑加固 (2026-04-13)

#### 🧱 时间块 1：GitHub 资产封板与文档同步 (2 小时)
* **任务**：将 Week 5 的总结笔记（上条回复已生成）正式提交至 `First_month` 仓库。
* **动作**：
    * 检查 `day26` 到 `day28` 的所有 Python 脚本，补齐 **Type Hints** 和 **Docstrings**。
    * 将 `zipper_table_logic.sql` 和 `explode_demo.sql` 的逻辑注释写清楚，尤其是“为什么 9999-12-31 是封锁位”。
* **物理意义**：在大厂，代码写完只算完成 50%，文档和注释补齐才叫“可交付资产”。

#### 🧱 时间块 2：弱点扫描与 SQL 逻辑盲区清除 (2 小时)
* **任务**：重新推演“连续登录”与“会话切分”这两个高频面试题。
* **动作**：
    * **手绘逻辑**：在草稿纸上模拟 `SUM(is_new_session) OVER(...)` 的累加过程。
    * **异常脑暴**：思考如果 `DataX` 同步过程中 MySQL 字段删除了，你的 Reader 配置文件会报错还是静默失败？
* **物理意义**：复盘不是看一眼，而是要在脑子里重新跑一遍底层物理逻辑。

#### 🧱 时间块 3：下周环境预热（静默模式） (1 小时)
* **任务**：不敲代码，只看文档，为明天的 HDFS 攻坚扫清术语障碍。
* **动作**：
    * 查阅 HDFS 架构图，确认你是否理解 **NameNode (元数据)** 与 **DataNode (实际块存储)** 的职责划分。
    * 
* **物理意义**：降低明天的“冷启动”阻力，让大脑在睡眠中预加载 HDFS 的概念。
------

### 🎯 Day 2 核心目标 Checklist（今日保底任务）

- [x] **指法固化**：继续 30 分钟纯正盲打练习，抵制看键盘的诱惑。
- [x] **算法进阶（双指针）**：用盲打+企业级规范，在 IDEA 中跑通 1 道核心算法题（移除元素）。
- [x] **SQL 联表**：完成 2 道基础的 `JOIN` 联表查询题。
- [ ] **Python 核心结构**：掌握字典（Dict）与集合（Set）的核心用法。
- [x] **企业级开源**：用带有 `feat/fix/docs` 前缀的规范化 Commit 提交今天的代码。

------

### ⏰ Day 2 详细执行日程与资源指南

#### 🌅 上午：指法修炼与双指针破局（9:00 - 11:30）

**【保底任务：约 2 - 2.5 小时】**

- **09:00 - 09:40 | ⌨️ 盲打特训（极其关键）**
  - **学习资源**：继续打开 Keybr.com 或 TypingClub.com。
  - **行动**：进行 30-40 分钟沉浸式练习。经过昨天的折磨，今天某些高频字母（如 a, s, e, t）应该稍微有一点点肌肉记忆了。继续保持**绝对不低头**的铁律。
- **09:40 - 10:20 | SQL 多表连接初探（2 道题）**
  - **学习资源**：牛客网 -> “SQL必知必会”或“SQL进阶挑战”。
  - **行动**：找 2 道涉及多表连接的题目。今天强迫自己全部使用 `INNER JOIN ... ON ...` 或 `LEFT JOIN ... ON ...` 的标准语法，不要用老旧的 `WHERE a.id = b.id` 来联表。把代码存入 `day02_sql_joins.sql`。
- **10:20 - 11:30 | 算法第二战：双指针思想**
  - **学习资源**：力扣 **第27题（移除元素）**。《代码随想录》-> 数组 -> 移除元素（重点看“双指针法”的动图演示）。
  - **行动**：在 IDEA 项目中新建 `day02_remove_element.py`。这道题是考察快慢指针的绝对经典。继续用极其缓慢的盲打，敲出带有 `List[int]` 和 Docstring 的企业级代码。

**【🔥 附加任务：如果你在 11:00 前完成】**

- **算法加餐（+30分钟）**：挑战力扣 **第977题（有序数组的平方）**。这同样是一道双指针神题（首尾指针向中间靠拢）。尝试自己先推演逻辑，想不出来再看《代码随想录》题解。

------

#### ☀️ 下午：Python 数据结构与函数进阶（14:00 - 17:30）

**【保底任务：约 2 - 2.5 小时】**

- **14:00 - 16:00 | Python 核心数据结构（字典与集合）**
  - **学习资源**：继续看 B站“黑马程序员 Python教程”。跳到**“字典 (Dictionary)”**和**“集合 (Set)”**的章节。
  - **行动**：在 IDEA 新建 `day02_dict_set.py`。字典是 Python 处理 JSON 数据（大数据开发中最常见的数据格式）的核心。边看视频边盲打练习：如何往字典里新增键值对？如何遍历字典的 `keys()` 和 `values()`？
- **16:00 - 17:30 | 函数与作用域**
  - **学习资源**：同上，跳到**“函数进阶”**（如果有讲到 `*args` 和 `**kwargs` 更好）。
  - **行动**：学习如何把一坨冗长的代码封装成一个干净的函数。体会局部变量和全局变量的区别。

**【🔥 附加任务：如果你在 16:30 前完成】**

- **极客扩展一：列表推导式（List Comprehension）（+30分钟）**：去百度搜“Python 列表推导式”。这是 Python 最具魅力的语法糖之一。尝试用一行代码 `[x**2 for x in nums if x > 0]` 替代原本需要四五行的 `for` 循环和 `if` 判断。
- **极客扩展二：字典的 get() 魔法（+15分钟）**：搜索“Python dict get 方法带默认值”。在处理不规范的脏数据时，`my_dict.get('age', 0)` 能避免程序直接报错崩溃，这在大数据清洗（ETL）中极其好用。

------

#### 🌙 晚上：规范化提交与复盘（19:00 - 21:00）

**【保底任务：约 1 小时】**

- **19:00 - 19:40 | Markdown 沉淀**
  - **行动**：在 `01_Daily_Logs` 文件夹下新建 `Day02.md`（盲打敲击）。总结一下双指针的核心思想，以及今天字典操作中遇到的任何缩进或语法坑。
- **19:40 - 20:30 | 第一次“企业级” Git 提交**
  - **行动**：打开 IDEA 终端，执行提交。**今天必须使用约定式提交（Conventional Commits）前缀！**
  - 依次输入（注意指法）：
    1. `git add .`
    2. `git commit -m "feat(algo): 完成移除元素双指针算法，掌握Python字典遍历"` （注意这里的 `feat:` 就是新特性的意思）
    3. `git push`

**【🔥 附加任务：如果你在 20:30 前完成】**

- **技术前瞻：深入探索 Git（+30分钟）**：在终端里敲击 `git log`，看看你这两天的提交记录有多么整齐漂亮。敲完按 `q` 退出。再去查一下 `git status` 是干什么用的。

------

